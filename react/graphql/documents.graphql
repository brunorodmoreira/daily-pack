query Documents(
  $acronym: String!
  $schema: String!
  $fields: [String]
  $where: String
) {
  documents(acronym: $acronym, schema: $schema, fields: $fields, where: $where)
    @context(provider: "vtex.store-graphql") {
    fields {
      key
      value
    }
  }
}
