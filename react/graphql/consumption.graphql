query DosageValues($productIds: [ID!]) {
  productsByIdentifier(field: sku, values: $productIds)
    @context(provider: "vtex.search-graphql") {
    items {
      itemId
    }
    properties {
      name
      values
    }
  }
}
